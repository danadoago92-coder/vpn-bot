# Copying is optional: python3 setup.py creates your private .env interactively.
# Values use JSON strings. Never source this file in a shell.
BOT_TOKEN=""
ADMIN_IDS=""
BRAND_NAME="فروشگاه کانفیگ"
WELCOME_TEXT="سلام {name} 👋\nبه {brand} خوش آمدید!"
DELIVERY_TEMPLATE="{name} عزیز، سرویس شما در {brand} آماده است ✅\n📦 سرویس: {plan}\n🧾 سفارش: {order_id}\n📅 اعتبار: {expires_at}"
CARD_NUMBER=""
CARD_HOLDER=""
SUPPORT_USERNAME=""
CHANNEL_USERNAME=""
TELEGRAM_PROXY_URL=""
DB_PATH="data/bot.sqlite3"
XUI_URL=""
XUI_USERNAME=""
XUI_PASSWORD=""
XUI_SUB_URL=""
XUI_VERIFY_TLS="true"

# Explicit global panel gate. false means manual delivery without panel API calls.
PANEL_CONNECTED="false"
# Optional round-robin cards as a JSON array encoded as a .env string; setup.py writes it safely.
CARDS_JSON="[]"

XUI_API_TOKEN=""
