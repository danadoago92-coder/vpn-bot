# اعتبارسنجی نسخه انتشار

- ۸۸ آزمون خودکار پاس شدند: دیتابیس تازه بدون لوکیشن و داده نمونه، لوکیشن کشور/مولتی‌لوکیشن داینامیک، پروتکل مستقل هر لوکیشن، تحویل لینک سابسکریپشن، تحویل فایل WireGuard، تراکنش کیف پول، تأیید تکراری، جلوگیری از پیام تکراری، پاک‌سازی منوها و استیکرهای موقت، کپی امن شماره کارت، موجودی هم‌زمان، رسید، کنترل مالکیت سرویس، مدیریت ادمین، کارت چرخشی، قطعی پنل، ستاپ و انتقال دیتابیس.
- `python -m py_compile` برای تمام فایل‌های Python اجرا شده است.
- فایل انتشار هیچ `.env`، `admins.json` واقعی، دیتابیس، رسید، توکن، رمز پنل یا محیط مجازی ندارد.
- در دیتابیس تازه هیچ کشور یا مولتی‌لوکیشنی از قبل ساخته نمی‌شود؛ ادمین نوع، نام، ایموجی و پروتکل‌های هر لوکیشن را تعیین می‌کند.
- 3x-ui فعلی ساخت خودکار کانفیگ عادی را انجام می‌دهد. WireGuard و OpenVPN در حالت دستی با فایل `.conf` و `.ovpn` تحویل می‌شوند.
